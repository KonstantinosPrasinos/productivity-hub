import styles from './NewCategory.module.scss';

const NewCategory = () => {
    return (<div className={`Stack-Container`}>
        <input type="text" class="Title Title-Input" placeholder="Add category name" />
        
    </div>);
}
 
export default NewCategory;